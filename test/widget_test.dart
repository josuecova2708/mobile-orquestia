import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Tests reales requieren mocks de Firebase y Provider.
    // Por ahora se omite el test de integración.
    expect(true, isTrue);
  });
}
